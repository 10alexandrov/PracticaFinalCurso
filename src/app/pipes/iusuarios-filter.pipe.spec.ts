import { IusuariosFilterPipe } from './iusuarios-filter.pipe';

describe('IusuariosFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IusuariosFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
